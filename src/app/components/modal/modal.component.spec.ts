import { ModalComponent } from './modal.component';

describe('ModalComponent', () => {
  let component: ModalComponent = new ModalComponent();

  beforeEach(() => {
    component = new ModalComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
