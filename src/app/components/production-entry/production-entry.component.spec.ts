import { ProductionEntryComponent } from './production-entry.component';

describe('ProductionEntryComponent', () => {
  let component: ProductionEntryComponent = new ProductionEntryComponent();

  beforeEach(() => {
    component = new ProductionEntryComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
