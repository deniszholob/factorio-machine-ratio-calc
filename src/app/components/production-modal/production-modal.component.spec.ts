import { ProductionModalComponent } from './production-modal.component';

describe('ProductionModalComponent', () => {
  let component: ProductionModalComponent = new ProductionModalComponent();

  beforeEach(() => {
    component = new ProductionModalComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
