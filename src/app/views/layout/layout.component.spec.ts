import { LayoutComponent } from './layout.component';

describe('LayoutComponent', () => {
  let component: LayoutComponent = new LayoutComponent();

  beforeEach(() => {
    component = new LayoutComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
