import { ProductionViewComponent } from './production-view.component';

describe('ProductionViewComponent', () => {
  let component: ProductionViewComponent = new ProductionViewComponent();

  beforeEach(() => {
    component = new ProductionViewComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
